# Byte-compiled
__pycache__/
*.py[codz]

# Packaging
build/
dist/
*.egg-info/
*.egg

# Virtualenvs
.venv/
venv/
env/

# Lab artifacts — never commit datasets or model weights
data/*
!data/.gitkeep
outputs/

# Test fixture is generated from tests/fixtures/build_gliner_tiny.py
# (auto-built on demand by tests/conftest.py), so the binary Arrow blobs
# are not committed.
tests/fixtures/gliner_tiny/

# Experiment tracking & secrets
wandb/
.env

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
